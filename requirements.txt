# core
streamlit==1.24.1

# data & ml
numpy>=1.24.3
pandas>=1.5.3
scikit-learn>=1.2.2
joblib>=1.2.0

# optional scientific dependency (only if you use it in notebook)
scipy>=1.10.1

# networking / API
requests>=2.32.3
python-dotenv>=1.0.1

# Colab / tunneling (only needed for Colab demos)
pyngrok>=5.2.0

# AI / LLM helpers (only if you actually call HF or Gemini)
huggingface-hub>=0.15.1
google-generativeai>=0.3.0

# visualization (if used in notebooks / app)
matplotlib>=3.7.0
seaborn>=0.12.2
